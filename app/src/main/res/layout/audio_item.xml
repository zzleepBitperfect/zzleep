<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:layout_marginBottom="5dp"
	android:orientation="horizontal"
	android:background="@drawable/button_audio"
	android:padding="10dp">

	<ImageView
		android:id="@+id/iconAudio"
		android:layout_width="40dp"
		android:layout_height="40dp"
		android:layout_marginRight="5dp"
		android:src="@mipmap/relax_audio"/>

	<LinearLayout
		android:layout_width="0dp"
		android:layout_height="wrap_content"
		android:layout_weight="1"
		android:orientation="vertical">

		<TextView
			android:id="@+id/audioName"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:text="Tantrica"
			android:textSize="18sp"/>


		<SeekBar
			android:id="@+id/seekbartimeline"
			android:layout_width="fill_parent"
			android:layout_height="wrap_content"

		/>
		<TextView
			android:id="@+id/audioPrice"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:layout_margin="0dp"
			android:ellipsize="end"
			android:maxLines="1"
			android:text="Gratis"
			android:textSize="12sp"/>
	</LinearLayout>

	<ImageView
		android:id="@+id/iconPlayAudio"
		android:layout_width="40dp"
		android:layout_height="40dp"
		android:src="@mipmap/btn_play"/>
</LinearLayout>